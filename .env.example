# Crypto keys — generate with:
#   openssl rand -hex 32
# and replace the placeholders below, or export them before `docker compose up`.
JWT_SECRET=change-me-please-use-openssl-rand-hex-32
JWT_REFRESH_SECRET=change-me-please-use-openssl-rand-hex-32-too